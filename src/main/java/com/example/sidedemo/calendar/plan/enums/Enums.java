package com.example.sidedemo.calendar.plan.enums;

public class Enums {
    public enum RepeatUnit {
        WEEKLY, MONTHLY, YEARLY;
    }

    public enum PlanType {
        SINGLE, RECURRING;
    }
}

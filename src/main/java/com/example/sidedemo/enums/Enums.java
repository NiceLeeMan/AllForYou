package com.example.sidedemo.enums;

public class Enums {
    public enum RepeatUnit {
        WEEKLY, MONTHLY, YEARLY;
    }

    public enum DisplayScope {
        PUBLIC, PRIVATE
    }
    public enum PlanType {
        SINGLE, RECURRING;
    }
}

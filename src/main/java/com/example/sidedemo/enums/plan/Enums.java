package com.example.sidedemo.enums.plan;

public class Enums {
    public enum RecurrenceType {
        WEEKLY,  // 매주 반복
        MONTHLY, // 매월 반복
        YEARLY,  // 매년 반복
        CUSTOM   // 특정 규칙 기반 반복
    }

    public enum DisplayScope {
        PUBLIC, PRIVATE
    }
}
